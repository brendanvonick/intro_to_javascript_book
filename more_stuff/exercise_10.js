let x = "5";
x = x + 1;      // returns "51"

let y = "5";
y++;

/*  y++ returns the value of 5. The increment operator coerces the string '5'
    to a number and then adds 1 to the value, however the increment operator
    returns the value prior to adding 1.

*/
